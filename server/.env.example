# --- Terminus core ---
PORT=3300
# Shared secret. The Android app sends this in its Cloud Sync "API key" field.
# Leave empty for localhost/Termux-only use; REQUIRED before exposing to the internet.
TERMINUS_API_KEY=

# Where local state snapshots are written before Drive archiving.
DATA_DIR=./data

# --- Brain chain ---
# Tier 1: local Ollama (phi3). On the phone this is the Termux daemon.
OLLAMA_URL=http://127.0.0.1:11434
# Tier 2: Claude backup. Get a key at https://console.anthropic.com
ANTHROPIC_API_KEY=
CLAUDE_MODEL=claude-sonnet-5
# Tier 3: Gemini last resort. Get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
# Cloud-GPU core: a big model on real GPUs via ANY OpenAI-compatible endpoint.
# Leads her brain chain when set (GPU_MODE=fallback to keep local phi3 first).
#   NVIDIA API Catalog (get an nvapi- key at https://build.nvidia.com):
#     GPU_API_BASE=https://integrate.api.nvidia.com/v1
#     GPU_MODEL=meta/llama-3.1-70b-instruct   -> fast, cheap, plenty smart (recommended)
#     GPU_MODEL=nvidia/nemotron-3-ultra-550b-a55b -> flagship reasoning; slow + credit-heavy,
#              raise GPU_MAX_TOKENS to ~8192 or replies can come back empty.
#   Also works with Together / Groq / Fireworks / OpenRouter / a vLLM box.
GPU_API_KEY=
GPU_API_BASE=https://integrate.api.nvidia.com/v1
GPU_MODEL=meta/llama-3.1-70b-instruct
GPU_MAX_TOKENS=2048
GPU_MODE=primary

# --- Telegram channel (talk to Kortana from ANY device) ---
# 1. In Telegram, message @BotFather -> /newbot -> follow prompts -> copy the token.
# 2. Paste it here and restart. Message your bot; she replies with her full brain.
# 3. She'll tell you your Telegram user id the first time — put it in ALLOWED_IDS
#    to lock her to just you (comma-separated for more than one person).
TELEGRAM_BOT_TOKEN=
TELEGRAM_ALLOWED_IDS=

# --- Google Drive archive (the 5TB Google One on a.i.intergrated.services@gmail.com) ---
# 1. Create an OAuth client (Desktop app) at https://console.cloud.google.com/apis/credentials
#    and enable the Google Drive API for the project.
# 2. Run `npm run auth` and sign in AS a.i.intergrated.services@gmail.com.
# 3. Paste the printed refresh token below.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=
# Root folder name created in that Drive:
DRIVE_ROOT_FOLDER=Kortana
