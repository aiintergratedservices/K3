/**
 * Value types used across the supervisor agent system.
 *
 * These are immutable records representing discovered entities,
 * configuration, and operational parameters.
 */
package com.hackerai.supervisor.model.value;
